import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Kayıt Ol - DockerChat',
  description: 'DockerChat hesabı oluşturun',
};

export default function RegisterPage() {
  return <RegisterForm />;
} 